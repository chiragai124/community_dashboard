node_modules/
.next/
out/
build/
dist/

# Local env — never commit credentials
.env
.env.local
.env*.local

# Runtime data: real weekly entries (weekly-entries.json) and the figures
# extracted from uploaded exports (imports.json). Both are machine-local state.
data/*.json
!data/demo-seed.json

*.tsbuildinfo
next-env.d.ts
.DS_Store
npm-debug.log*
